## 目标
- 删除 `backend/app.py` 中自定义的 `POST /agent/run`，仅使用 `add_langgraph_fastapi_endpoint(app, graph, "/agent")` 暴露后端服务。
- 调整 pytest：移除依赖 `/agent/run` 的 API 测试，保留图级测试。

## 改动说明
- `backend/app.py`
  - 去掉 `@app.post("/agent/run")` 路由及其实现
  - 将 `ag_ui_langgraph` 设为强依赖：直接导入，导入失败抛错提示安装
  - 保留 `app = FastAPI()` 与 `add_langgraph_fastapi_endpoint(app, graph, "/agent")`
- `tests/test_backend.py`
  - 删除 `test_api_success_flow` 用例与对 `backend.app` 的引用
  - 保留：`test_graph_success_flow`、`test_graph_failure_flow_human_review`

## 验证步骤
1. 运行 `pytest`，确保图级测试通过
2. 启动服务（需安装 `ag-ui-langgraph`）：`uvicorn backend.app:app --port 8001`
3. 前端或手工验证 `/agent` SSE 端点可用（可选）

## 风险与提示
- 未安装 `ag_ui_langgraph` 将无法启动服务：根据异常提示安装该依赖
- 原有依赖 `/agent/run` 的脚本会失效：需要迁移到 SSE 端点

## Implementation Checklist
1. 删除 `backend/app.py` 中的 `POST /agent/run`
2. 强依赖 `ag_ui_langgraph` 并注册 `/agent` 端点
3. 更新 `tests/test_backend.py` 移除 API 测试
4. 运行 `pytest` 验证通过